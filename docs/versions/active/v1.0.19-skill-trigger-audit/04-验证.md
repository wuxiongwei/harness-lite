# 04-验证.md · v1.0.19

> **路径B · 阶段2/2**

---

## 验证矩阵

| # | 验收项 | 方式 | 结果 |
|---|------|-----|------|
| T1 | path-a 全部"X/5"格式 | grep | ✅ 5 标题 + 5 末尾 |
| T2 | path-c 保持"X/4"（4 阶段不变） | grep | ✅ 不动 |
| T3 | path-b 保持 2 阶段（不变） | grep | ✅ 不动 |
| T4 | skill body 无"4 阶段"残留 | grep | ✅ 0 处 |
| T5 | templates/CLAUDE.md 与 CLAUDE.md 同步 | grep | ✅ 全 5 阶段 |
| T6 | stock 现场同步 | grep stock CLAUDE.md | ✅ 全 5 阶段 |
| T7 | stock validate 全绿 | tail validate | ✅ |

---

## skill 触发能力评估（静态）

虽不能在 H-L 自身仓库直接启 claude REPL（已在另一 session 里），但通过审计 skill description 评估"能否被自动触发"：

| skill | description 第一句 | 触发能力评估 |
|------|-------------------|------------|
| harness-req | "主入口。当用户描述新需求 / 改文案 / 修 bug 时调用" | ✅ 触发场景明确 |
| harness-design | "设计阶段 skill。基于 01-需求.md 生成 02-设计.md" | ✅ 上下文清晰 |
| harness-review | "一致性审计。检查产物链路 5 维度一致性" | ✅ 任务明确 |
| harness-impact | "影响面分析。基于 git diff..." | ✅ 触发动作明确 |
| harness-test-ci | "全量回归 + CI 质量门禁" | ✅ 触发时机明确 |

5 个 skill description 都符合 v1.0.7 沉淀的 schema 约定（≤300 字符 / 含触发场景 + 核心能力 / 动词开头）。

→ **真实触发验证**留给用户在 stock 实战中自行验证（无法在主 session 中直接启另一个 REPL）。

---

## 反向校验

- [x] 改动针对真实问题（path-a 5 阶段事实散布在 13 处的不一致）
- [x] 同根 v1.0.5-18 的 schema/承诺/行为漂移类——这次是"事实陈述漂移"
- [x] stock 现场已同步（path-a/b 模板 + skill body + CLAUDE.md）
- [x] 不破坏 path-c（保持 4 阶段事实）
- [x] 用户在 stock 重新启 claude 时，skill 会自动加载新版本（描述更准确）

---

## 元发现

### M1 · 这是同根第 7 次

| # | 同根类型 | 修复版本 |
|---|--------|---------|
| 1 | settings.json schema 漂移 | v1.0.5 |
| 2 | skill/agent frontmatter | v1.0.7 |
| 3 | uninstall.sh 残留 | v1.0.8 |
| 4 | 模板占位符未渲染 | v1.0.11 |
| 5 | hook if 字段格式 | v1.0.12 |
| 6 | upgrade.sh local 关键字 | v1.0.18 |
| 7 | **path-a 5 阶段未横向同步** | **v1.0.19** |

每次都是"改一处忘了同步另几处"。validate.sh 抓不到这种"语义层"漂移——只能靠**主动审计**或**dogfooding 暴露**。

### M2 · skill body 也是审计范围

之前 v1.0.7 改了 skill frontmatter（`trigger`/`version` 删除），但**body 里的"4 阶段"等过时表述潜伏到 v1.0.19 才被发现**。

→ 加入 backlog："修元数据时，body 也要顺便扫一遍"——加到 Adding-anything checklist。

### M3 · 真实触发验证仍是盲区

5 个 skill 静态 description 看起来都对，但**没人在真 claude REPL 真敲过**：
- 我不能在主 session 内启另一个 REPL
- 用户 stock 实战时才会真触发

→ 这层验证留给 stock 实战反馈。如果用户敲 `/harness-req` 不被识别，那是下一个 bug。

---

## 门控

```
状态: 🟢 PASS（静态审计 + 实战预备）
```

---

*Harness-Lite v1.0.19 · 路径B · 阶段2/2 · ✅ 完成*
