# 04-回归.md — v1.0.1 完整回归测试

> **路径C · 阶段4/4** · 完整回归 + 真实环境验证

---

## 元信息

- Bug ID：v1.0.1-INIT-001~004
- 追溯：[03-修复.md](03-修复.md)
- 状态：🟢 已通过

---

## ⚠️ 路径C关键硬规则

**最终验证必须回到用户原始失败场景！**

回到 01-复现.md §3 的 4 个 bug 复现场景，逐一验证修复后是否消失。

---

## 1. 回归测试

### 1.1 Layer 1 单元测试
不适用（产品是 Bash 脚本，无单测框架）。

### 1.2 Layer 2 集成测试

测试目标：完整 install→validate→uninstall 流程

```bash
# 测试1：干净安装
rm -rf /tmp/harness-test && mkdir -p /tmp/harness-test
cd /tmp/harness-test && git init -q
echo '{"name":"test"}' > package.json
cat <<EOF | bash $HARNESS_LITE/scripts/init.sh
2
test
desc
desc
3
new
n
EOF
# 实测结果：✅ 无 sed 错误、无 unary operator、banner 正常

# 测试2：验证安装
bash $HARNESS_LITE/scripts/validate.sh
# 实测结果：✅ 所有检查通过

# 测试3：卸载
echo "YES" | bash $HARNESS_LITE/scripts/uninstall.sh
# 实测结果：✅ 已清理空的 .claude/ 目录

# 测试4：用户文件保留
[ -f /tmp/harness-test/package.json ] && echo "✅ 用户文件保留"
[ -d /tmp/harness-test/team-knowledge ] && echo "✅ team-knowledge保留"
[ ! -e /tmp/harness-test/.claude ] && echo "✅ .claude/ 已彻底清理"
```

### 1.3 Layer 3 E2E 真实链路

✅ 上述 Layer 2 测试已经在真实文件系统执行（不是 mock）。

⚠️ 真正的 Layer 3（在真实业务项目验证）将在 stock_make_money 试用阶段进行。

---

## 2. 原 Bug 场景验证（关键！）

### Bug #1: sed 转义错误

```
原期望：Step 5 完成所有变量渲染
修复前：sed: bad flag in substitute command
修复后：✅ 无错误，CLAUDE.md/tech-stack-rules.md 正确渲染
证据：grep -l "{{" /tmp/harness-test/CLAUDE.md → 无输出（已渲染）
```

### Bug #2: .claude/ 残留

```
原期望：卸载后 .claude/ 完全消失
修复前：.claude/ 空目录残留
修复后：✅ 完全清理
证据：ls .claude → ls: .claude: No such file or directory
```

### Bug #3: unary operator 错误

```
原期望：init.sh 干净结束，无错误
修复前：line 471: [: -eq: unary operator expected
修复后：✅ 干净退出
证据：测试输出无 "unary operator" 字符串
```

### Bug #4: banner ANSI 未渲染

```
原期望：banner 中颜色正常显示
修复前：显示原始 \033[1;33m... 字符串
修复后：✅ 简化为纯文本+log_info组合，颜色正常
证据：测试输出无 \033 字面量
```

---

## 3. 同类入口验证

| 入口 | 验证步骤 | 结果 |
|------|---------|-----|
| init.sh | 完整流程 | ✅ |
| validate.sh | 完整流程 | ✅ |
| uninstall.sh | 完整流程 | ✅ |

---

## 4. 副作用回归

- ✅ 用户文件（package.json/src/）零修改
- ✅ team-knowledge/ 完整保留
- ✅ 备份机制正常
- ✅ git 仓库未受影响

---

## 5. 反向校验

- [x] **场景还原**：4个bug都回到了 01-复现 的原始场景
- [x] **现象消失**：所有原 bug 现象消失
- [x] **同类完整**：所有 sed 调用统一为 # 分隔符
- [x] **副作用**：无影响其他功能
- [x] **数据兼容**：无数据迁移问题

---

## 6. 知识沉淀（路径C必做）

- [x] **写入 pitfall**：sed 分隔符冲突这个教训值得沉淀
  - 待写入 `team-knowledge/wiki/pitfalls/001-sed-delimiter-conflict.md`
- [x] **更新 tech-stack-rules**：是否在 tech-stack-rules 加 Bash 规则？
  - 决策：暂不加（项目本身就是 Bash 脚本，不是约束业务用 Bash）
- [x] **CHANGELOG 更新**：v1.0.1 条目待加

---

## 门控信号

```
状态: 🟢 PASS
检查时间: 2026-06-12
最终决策人: 产品作者（人介入：实测确认 + 准备commit）
```

### 通过条件
- [x] Layer 2 全部通过
- [x] 4 个 bug 原场景已验证修复
- [x] 副作用回归通过
- [x] 知识已规划沉淀（待执行）
- [x] 反向校验全部Yes

---

## 下一步

✅ → v1.0.1 修复完成

后续动作：
1. 写 pitfall: `team-knowledge/wiki/pitfalls/001-sed-delimiter-conflict.md`
2. 更新 CHANGELOG.md
3. 更新 VERSION → 1.0.1
4. git init + 首次 commit（dogfooding 完成 + bug 修复一起入库）
5. 归档：移到 `docs/versions/archive/v1.0.1-fix-init-bugs/`

---

## 🎉 Dogfooding 元发现

**首次用 harness-lite 修 harness-lite 自己的 bug，发现的事**：

1. ✅ **流程跑得通**：路径C 4阶段（复现→根因→修复→回归）能完整走完
2. ✅ **强制 RED 测试设计 → GREEN 验证 → 副作用检查**这个流程真的能逼出更深的思考
3. ✅ **反向校验追问**让我对每个改动都更踏实
4. 🟡 **路径C对小bug显得"重"**：4个P3级cosmetic bug也走4阶段，有点过度。未来可考虑：
   - 多bug合并修复时，可以一份产物一次处理
   - 极小bug（typo级）允许走路径B
5. 🟡 **缺少自动化回归测试**：每次都手动跑 /tmp/harness-test 流程，应该写成脚本

这些发现已记入 backlog：
- proposal: 增加 `tests/test-init-uninstall.sh` 自动化回归脚本
- proposal: 路径C允许"批量bug合并"模式

---

*Harness-Lite v1.0.1 · 路径C · 阶段4/4 · 🎉 第一次 dogfooding 完成！*
