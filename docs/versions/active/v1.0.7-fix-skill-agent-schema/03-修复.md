# 03-修复.md · v1.0.7

> **路径C · 阶段3/4**

---

## 改动清单

| 文件 | 类型 | 改动 |
|-----|-----|-----|
| `templates/.claude/skills/harness-req/SKILL.md` | 重写 frontmatter + 调用名 | 删 trigger/version，融合到 description；标题/示例 /harness:req → /harness-req |
| `templates/.claude/skills/harness-design/SKILL.md` | 同上 | 同上 |
| `templates/.claude/skills/harness-impact/SKILL.md` | 同上 | 同上 |
| `templates/.claude/skills/harness-review/SKILL.md` | 同上 | 同上 |
| `templates/.claude/skills/harness-test-ci/SKILL.md` | 同上 | 同上 |
| `templates/.claude/agents/doc-generator.md` | 重写 frontmatter | 删 visible/invisible，加 tools，可见范围下放到 system prompt body |
| `templates/.claude/agents/implementer.md` | 同上 | 同上 |
| `templates/.claude/agents/reviewer.md` | 同上 | 同上 |
| `templates/.claude/agents/validator.md` | 同上 | 同上 |
| `templates/CLAUDE.md` | 全局替换 | 5 处 /harness: → /harness- |
| `scripts/init.sh` | 全局替换 | 1 处提示语修正 |
| `scripts/validate.sh` | 新增 schema 校验段 | 50 行 grep 校验：trigger/version/visible/invisible/缺 description/旧调用名 |
| `CLAUDE.md` (仓根) | 全局替换 | 1 处 |
| `README.md` (仓根) | 全局替换 | 1 处 |
| stock_make_money | 同步现场 | skills + agents 目录覆盖；CLAUDE.md 内 /harness: → /harness- |
| `VERSION` | 1.0.6 → 1.0.7 | bump |
| `CHANGELOG.md` | 追加 [1.0.7] | 描述 9 个 frontmatter 重写 + 调用名修正 + validate 扩展 |

---

## 关键代码：skill frontmatter 新格式

```yaml
---
name: harness-req
description: |
  Harness-Lite 主入口。当用户描述新需求 / 改文案 / 修 bug 时调用，
  自动判定路径（A 完整需求 / B 小需求 / C Bug 修复），生成第一阶段产物。
  典型触发：用户说"新需求"、"改个 X"、"加一个 X"、"修复 bug"...
---
```

**关键变化**：
- 删 `trigger.keywords`（自造字段，Claude Code 不认）
- 删 `trigger.patterns`（同上）
- 删 `version`（产品版本号已在 settings.json `_version`）
- 把"何时触发"语义全部融入 `description`——这才是 Claude 决定何时自动调用的依据

---

## 关键代码：agent frontmatter 新格式

```yaml
---
name: reviewer
description: |
  独立评审专员。对 doc-generator 起草的产物做独立评审。
  关键是"独立 context"——不看起草人的思路过程，只看最终产物 + 上游需求...
tools: Read, Glob, Grep
---

# reviewer subagent

> ...

## 工作约束（系统提示词层面）

**只读范围**：
- `docs/versions/active/{current}/**`（最终产物）
- `.claude/rules/**`（规则约束）
- `team-knowledge/wiki/pitfalls/**`（历史踩坑）

**禁止读取**：
- doc-generator 的中间思考过程（保持独立 context）
- `src/main/**`
- 其他 subagent 的执行日志
```

**关键变化**：
- 删 `visible:` / `invisible:`（自造字段）
- 加 `tools:`（官方支持，限制实际工具调用）
- 把"可见 / 不可见"语义下放到 system prompt body——LLM 看到这部分文字会按指示约束自己读什么

---

## 关键代码：validate.sh schema 校验扩展

```bash
# v1.0.6 已有：settings.json 5 条不变式
# v1.0.7 新增：skill / agent frontmatter 校验
+ 检查 skill 不含 trigger:
+ 检查 skill 不含 version:
+ 检查 agent 不含 visible: / invisible:
+ 检查 skill 含 description:（触发关键）
+ 检查全文档不残留 /harness:
```

反向测试已确认：人为造坏 5 类错误，validate.sh 全部抓到。

---

## 关键代码：调用名修正

```diff
- /harness:req      # ❌ 冒号是 plugin 命名空间专用
+ /harness-req      # ✅ 项目级 skill 调用名 = 目录名

- /harness:design   → /harness-design
- /harness:impact   → /harness-impact
- /harness:review   → /harness-review
- /harness:test-ci  → /harness-test-ci
```

实证：57 处全局替换完成（49 处在 .claude/ 内 + 8 处在外部），stock_make_money 现场同步完成。

---

## 现场修复

stock_make_money 同步：
```bash
cp -r templates/.claude/skills/.   /…/stock_make_money/.claude/skills/
cp -r templates/.claude/agents/.   /…/stock_make_money/.claude/agents/
sed -i 's#/harness:#/harness-#g'   /…/stock_make_money/CLAUDE.md
```

settings.json / hooks / templates 都不动（这些不依赖本次 schema 变更）。

---

## 自检

- [x] 9 个文件 frontmatter 全部按官方 schema
- [x] 49 + 8 处 `/harness:` 全部替换为 `/harness-`
- [x] validate.sh 反向测试通过（人为破坏能抓出）
- [x] stock_make_money 现场跑 validate.sh 全绿
- [x] 历史版本目录（v1.0.{1,2,3,5,6}-*/）保持不动（属于历史档）

---

*Harness-Lite v1.0.7 · 路径C · 阶段3/4*
