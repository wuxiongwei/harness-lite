# 04-验证.md · v1.0.3

> **路径B · 阶段2/2** · 实施 + 手测验收

---

## 元信息

- **版本**：v1.0.3-tier-defaults
- **完成日期**：2026-06-15
- **状态**：🟢 已完成

---

## 1. 实施记录

### 改动 1 · `scripts/init.sh::interactive_tier()`

```diff
-  1) Lite     - 单兵/小团队 (1-3人)   [v1.3 计划支持，当前不可用]
+  1) Lite     - 单兵/小团队 (1-3人)   [✅ 当前支持]

   case $tier_choice in
       1)
-          log_warn "Lite档位 v1.3 才支持，当前安装为 Standard 档"
-          TIER="standard"
+          TIER="lite"
+          log_success "已选择：Lite 档（1-3人）"
```

### 改动 2 · `scripts/init.sh::interactive_project_info()`

```diff
+  local default_project_name=$(basename "$TARGET_DIR")
+  local default_team_size
+  case "$TIER" in
+      lite)     default_team_size="1-3人" ;;
+      standard) default_team_size="4-10人" ;;
+  esac

-  read -p "项目名（如 my-awesome-app）: " PROJECT_NAME
-  PROJECT_NAME=${PROJECT_NAME:-"我的项目"}
+  read -p "项目名（默认 $default_project_name）: " PROJECT_NAME
+  PROJECT_NAME=${PROJECT_NAME:-"$default_project_name"}

-  read -p "团队规模（如 5人，3前端2后端）: " TEAM_SIZE
-  TEAM_SIZE=${TEAM_SIZE:-"4-10人"}
+  read -p "团队规模（默认 $default_team_size）: " TEAM_SIZE
+  TEAM_SIZE=${TEAM_SIZE:-"$default_team_size"}
```

### 改动 3 · `VERSION` & `CHANGELOG.md`

- VERSION：`1.0.2` → `1.0.3`
- CHANGELOG：新增 `[1.0.3] - 2026-06-15` 节，包含 Added（Lite 解锁）+ Changed（默认值优化）

---

## 2. 手测矩阵

测试场景：模拟用户全程空回车（极端"开箱即用"路径）

| 场景 | 输入 | 期望 | 实测 |
|------|-----|------|-----|
| Lite 档 + 全默认 | `1\n\n\n\n\n\nN\n` | TIER=lite, 项目名=目录名, 团队规模=1-3人 | ✅ 全部命中 |
| Standard 档 + 全默认 | `2\n\n\n\n\n\nN\n` | TIER=standard, 项目名=目录名, 团队规模=4-10人 | ✅ 全部命中 |
| 语法检查 | `bash -n init.sh` | 无报错 | ✅ |
| validate.sh | 装完后跑 | 全绿 | ✅ |

### 实证摘录

**Lite 档**（`/tmp/harness-lite-v103-test`，目录名为 `harness-lite-v103-test`）：
```
# harness-lite-v103-test          ← 项目名取目录名 ✅
- **团队规模**：1-3人               ← Lite 档默认值 ✅
- **当前阶段**：maintaining
```

**Standard 档**（`/tmp/harness-lite-v103-std`）：
```
# harness-lite-v103-std            ← 项目名取目录名 ✅
- **团队规模**：4-10人              ← Standard 档默认值 ✅
```

---

## 3. 反向验证

- [x] 用户原话两件事都做了：Lite 档解锁 + 项目名默认值
- [x] 没引入 breaking change：已装项目（v1.0.2）不受影响（TIER 字段是 init.sh 内部状态，不存盘）
- [x] 改动文件数 = 3，符合路径 B 硬规则
- [x] 装完产物 CLAUDE.md 渲染正确，validate.sh 全绿

---

## 4. 已知遗留 / 进化点

1. **Banner 仍显示 v1.0.2**——install.sh 末尾的 `Harness-Lite v$HARNESS_LITE_VERSION` 是从 `VERSION` 文件读的，下次安装会自动是 v1.0.3。本次手测时机在 bump 前，不算 bug
2. **CLAUDE.md 模板里的 `{{TEAM_SIZE}}`** 已正确渲染，但 Lite/Standard 在 CLAUDE.md 文案上没差异——这是有意的（principles §2 最小化）。如果将来 Lite 需要不同的工作流（比如略过 review），再分模板
3. **PROJECT_TAGLINE 默认 = PROJECT_NAME** 体感重复（首页显示两遍同样的文字）。本次不动——下次有人反馈再改

---

## 门控信号

```
状态: 🟢 PASS
```

### 通过条件
- [x] 实施完成（init.sh + VERSION + CHANGELOG）
- [x] 手测两档全绿（Lite + Standard）
- [x] validate.sh 通过
- [x] 反向验证通过

---

*Harness-Lite v1.0.3 · 路径B · 阶段2/2 · ✅ 完成*
