# macOS
.DS_Store
._*

# 编辑器
.vscode/
.idea/
*.swp
*~

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# 临时文件
*.tmp
*.bak
*.log

# 内部测试数据（不入开源版）
examples/internal-demo/
test-output/

# 用户自己的反馈（不入产品仓）
backlog/private-*.md

# v1.0.21 · 决策学习相关（个人偏好不入团队仓库）
.claude/decision-log.md
.claude/auto-decide.md
# v1.0.24 · 处理符号链接边界（H-L 仓库 dogfooding 时 .claude → templates/.claude）
templates/.claude/decision-log.md
templates/.claude/auto-decide.md

# v1.0.23 · 多人协同相关（团队共享，**不**忽略）
# .claude/branch-strategy.md       入仓库（团队约定）
# .claude/assignments/*.md         入仓库（任务认领板）
# 与 v1.0.21 个人偏好相反，协同需要团队可见

# 注：.claude 是符号链接到 templates/.claude（dogfooding）
# 让 git 跟踪符号链接本身（以路径形式存储），不忽略
# 这样 clone 后符号链接保留，dogfooding 配置同步
