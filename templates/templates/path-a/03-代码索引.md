# 03-代码索引.md

> **路径A · 完整需求 · 阶段3/4**
> 受众：执行者本人 + 后续维护者 + Code Reviewer
> 目的：编码过程中维护的"代码地图"，防止改动失控

---

## 元信息

- **版本**：v{{VERSION}}-{{SLUG}}
- **创建日期**：{{DATE}}
- **创建人**：{{AUTHOR}}
- **追溯**：[02-设计.md](02-设计.md)
- **状态**：⬜ 草稿 / 🟡 编码中 / 🟢 编码完成 / 🔴 已阻塞

---

## 1. 实施清单（边写边维护）

### 1.1 新增文件

| 文件路径 | 内容简述 | 关联设计 | 状态 |
|---------|---------|---------|-----|
| `path/to/UserService.java` | 用户服务 | 02-设计 §3.3 | ⬜ 待写 / 🟡 写中 / 🟢 已完成 |
| `path/to/UserDTO.java` | 用户DTO | 02-设计 §4.1 | ⬜ |

### 1.2 修改文件

| 文件路径 | 改动内容 | 关联设计 | 状态 |
|---------|---------|---------|-----|
| `path/to/UserController.java` | 新增 createUser 接口 | 02-设计 §3.3 | ⬜ |

### 1.3 删除文件

| 文件路径 | 删除原因 | 关联设计 | 状态 |
|---------|---------|---------|-----|
| `path/to/Obsolete.java` | 被 NewClass 替代 | 02-设计 §3.1 | ⬜ |

### 1.4 配置/资源变更

| 路径 | 改动内容 | 状态 |
|------|---------|-----|
| `application.yml` | 新增 user.cache.ttl 配置 | ⬜ |
| `db/V001__create_users.sql` | 新建 users 表 | ⬜ |

---

## 2. 关键代码片段（重要节点）

> 不是粘贴所有代码，是**关键决策点**的代码

### 2.1 核心算法 - {标题}

```java
// 路径: path/to/CoreLogic.java:42-58
public Result process(Input input) {
    // ... 核心逻辑
}
```

**为什么这么写**：
（解释为什么用这种实现，不用其他实现）

### 2.2 边界处理 - {标题}

```java
// 路径: path/to/X.java:23-30
if (input == null || input.isEmpty()) {
    // 特殊处理
}
```

**对应需求**：01-需求 §边界场景1

### 2.3 易错点 - {标题}

```java
// 路径: path/to/Y.java:15-20
// ⚠️ 注意：此处使用悲观锁而非乐观锁
// 原因：业务铁律 [[扣款必须用悲观锁]]
```

---

## 3. 编码过程日志

> 重大决策、踩坑、变更原因，按时间顺序记录

### {{TIMESTAMP}} - 开始编码

- 按设计稿启动
- 选择从 ClassA 开始

### {{TIMESTAMP}} - 发现 X

- 现象：（编码中发现的事）
- 影响：（对设计的影响）
- 决策：（怎么处理）

### {{TIMESTAMP}} - 编码完成

- 完成时间：
- 实际改动文件数：
- 与设计预估的偏差：

---

## 4. 变更追溯（与设计对照）

### 4.1 与02-设计完全一致的部分

✅ 已实现：
- ClassA → 按设计 §3.3
- funcB → 按设计 §3.3
- 接口X → 按设计 §4.1

### 4.2 与02-设计有偏差的部分（需说明）

⚠️ 偏差项：

#### 偏差1: {标题}

- **设计原方案**：（02-设计中怎么写的）
- **实际实现**：（实际怎么实现的）
- **偏差原因**：（为什么不按原设计）
- **是否回写设计**：✅ 已更新02-设计 / ⬜ 待回写

#### 偏差2: {标题}

（同上格式）

### 4.3 设计未提及但代码做了的（必须警惕！）

🚨 警告项：

如果代码做了设计没说的事，**必须**：
1. 列在这里
2. 判断是否合理
3. 不合理 → 立即删除/回退
4. 合理 → 立即回写到02-设计.md

```
列表：
- (无 / 有：列出来)
```

---

## 5. lint / 静态检查结果

```
执行命令：（如 mvn checkstyle:check 或 npm run lint）
执行时间：
执行结果：
  ✅ Pass / ⚠️ Warning x N / ❌ Error x N

失败项处理：
- 项目1：已修复 / 跳过原因
- 项目2：...
```

---

## 6. 反向校验（提交前必填）

- [ ] **设计追溯**：所有改动文件能追溯到02-设计的某节吗？
  > 改动1 ← 设计 §X.Y
  > 改动2 ← 设计 §X.Y

- [ ] **越界检查**：是否有"设计没要求但代码做了"的事？
  > 检查结果：（无 / 有：列在 §4.3）

- [ ] **范围检查**：是否动了"不做的范围"中的文件？
  > 检查结果：（无 / 有：原因说明）

- [ ] **lint通过**：静态检查全部通过吗？
  > 结果：✅ / ❌

- [ ] **review准备**：是否准备好交给独立的 code-reviewer subagent？
  > diff 范围：
  > 关键文件：

---

## 7. 关联文档

- 上游：[02-设计.md](02-设计.md)
- 下游：[04-测试.md](04-测试.md)
- 关联wiki：[[相关代码模块]]

---

## 门控信号

```
状态: 🟢 PASS / 🟡 REVISION / 🔴 BLOCKED
检查时间: {{TIMESTAMP}}
Reviewer: {{REVIEWER}}（独立 code-reviewer subagent）
```

### 通过条件（人介入3：Review决策）
- [ ] 所有改动文件都在实施清单中
- [ ] 反向校验五项全部Yes
- [ ] lint/静态检查通过
- [ ] code-reviewer subagent 审查通过（严重项=0）
- [ ] 设计偏差已回写或决策

### 阻塞原因（如有）
（如有 lint/review 严重项）

---

## 下一步

- ✅ 状态 = 🟢 → 进入阶段4：[04-测试.md](04-测试.md)
- 🟡 状态 = 🟡 → 修复后重新review
- 🔴 状态 = 🔴 → 等待阻塞解除

---

*Harness-Lite v{{HARNESS_LITE_VERSION}} · 路径A · 阶段3/4*
