# 01-复现.md — v1.0.1 修复 init.sh 的多个小Bug

> **路径C · Bug修复 · 阶段1/4** · Harness-Lite 第一次 dogfooding 产物
> Bug 来源：在 /tmp/harness-test 测试 init.sh 时发现

---

## 元信息

- **Bug ID**：v1.0.1-INIT-001~004
- **创建日期**：2026-06-12
- **报告人**：产品作者（自测发现）
- **严重等级**：1个P0已修，3个P2-P3待修
- **状态**：🟡 已复现（部分已修）

---

## 1. Bug 清单

### Bug #1（P0，✅已修）：sed 转义错误中断脚本

```
错误：sed: 1: "s|{{TECH_STACK}}|Node.j ...": bad flag in substitute command: '�'
影响：脚本在 Step 5 渲染 CLAUDE.md 时崩溃
```

### Bug #2（P2，待修）：uninstall 后 .claude/ 空目录残留

```
卸载后 /tmp/harness-test/.claude 仍是空目录
预期：完全卸载应该删掉
```

### Bug #3（P3，待修）：init.sh 末尾报错 unary operator

```
init.sh: line 471: [: -eq: unary operator expected
原因：$needs_backup 在 print_next_steps 函数内访问不到（变量作用域）
```

### Bug #4（P3，待修）：Banner 颜色变量未生效

```
显示原始：\033[1;33mcp ...\033[0m
原因：cat << EOF 不解析 ANSI 转义码
```

---

## 2. 复现步骤

### 通用环境
- macOS · BSD sed · Bash 3.2

### 复现命令

```bash
rm -rf /tmp/harness-test && mkdir -p /tmp/harness-test
cd /tmp/harness-test && git init -q
echo '{"name":"test"}' > package.json

cat <<EOF | bash /path/to/init.sh
2
test
描述
描述
3人
new
n
EOF

# Bug #1：观察 sed 错误（已修）
# Bug #3：观察 init.sh 末尾的 unary operator
# Bug #4：观察 banner 中的原始 ANSI 字符串

# Bug #2 复现
echo "YES" | bash /path/to/uninstall.sh
ls -la /tmp/harness-test/.claude  # 空目录残留
```

### 复现频率

所有 4 个 bug 在 macOS 下 100% 必现。

---

## 3. 影响范围

| Bug | 影响用户 | 影响入口 |
|-----|---------|---------|
| #1 | 首次安装所有用户 | init.sh |
| #2 | 所有卸载用户 | uninstall.sh |
| #3 | 所有 init.sh 用户 | init.sh（cosmetic） |
| #4 | 所有 init.sh 用户 | init.sh（cosmetic） |

数据影响：✅ 无脏数据，无需数据修复

---

## 4. 失败回归测试设计

```bash
#!/bin/bash
# tests/test-init-uninstall-regression.sh

set -e
TEST_DIR=/tmp/harness-test-regression
rm -rf $TEST_DIR && mkdir -p $TEST_DIR
cd $TEST_DIR && git init -q
echo '{"name":"test"}' > package.json

# 准备答案
ANSWERS=$(cat <<EOF
2
test
desc
desc
3
new
n
EOF
)

# 测试 #1: init.sh 干净退出（无 sed 错误）
INIT_OUTPUT=$(echo "$ANSWERS" | bash $HARNESS_LITE/scripts/init.sh 2>&1)
echo "$INIT_OUTPUT" | grep -q "bad flag" && echo "FAIL #1" && exit 1

# 测试 #4: banner 不含原始 ANSI 字符串
echo "$INIT_OUTPUT" | grep -qE '\\033\[' && echo "FAIL #4: banner 含 raw ANSI" && exit 1

# 测试 #3: init.sh 末尾不报 unary operator
echo "$INIT_OUTPUT" | grep -q "unary operator" && echo "FAIL #3" && exit 1

# 跑 uninstall 测试 #2
echo "YES" | bash $HARNESS_LITE/scripts/uninstall.sh

# 测试 #2: .claude/ 必须不存在
[ -e $TEST_DIR/.claude ] && echo "FAIL #2: .claude/ 残留" && exit 1

echo "✅ 全部4个测试通过"
```

当前状态：Bug #2 #3 #4 未修复 → 测试 RED ❌

---

## 5. 反向校验

- [x] **复现校验**：4个bug都能稳定复现
- [x] **范围校验**：影响 macOS 所有用户
- [x] **测试校验**：RED 测试已设计（见 §4）
- [x] **理解校验**：一句话总结
  > Bug #1 是 sed 引发崩溃（已修）；#2-4 是脚本细节问题影响体验

---

## 门控信号

```
状态: 🟢 PASS
检查时间: 2026-06-12
```

### 通过条件
- [x] Bug 稳定复现
- [x] 影响范围确认
- [x] RED 测试已设计
- [x] 反向校验全部Yes

---

## 下一步

✅ → 进入阶段2：[02-根因.md](02-根因.md)

---

*Harness-Lite v1.0.1 · 路径C · 阶段1/4 · 🎉 第一次 dogfooding！*
