# 04-回归.md · v1.0.8

> **路径C · 阶段4/4**

---

## 验证矩阵

| 项 | 方式 | 结果 |
|---|------|-----|
| 语法 | `bash -n uninstall.sh` | ✅ |
| 装完文件数 | `find .claude -type f \| wc -l` | 26 |
| 卸载残留 | 同上重跑 | 0（`.claude/` 已清理）|
| 用户数据保留 | 检查 `CLAUDE.md / docs / team-knowledge` | ✅ 全部保留 |
| 备份生成 | 检查 `.harness-lite-uninstall-backup/{ts}/` | ✅ |

---

## 反向校验

- [x] **复现可重现**：v1.0.7 100% 残留 5 个，v1.0.8 100% 干净
- [x] **修复在直接层**：补清单，不引入新机制
- [x] **没引入新风险**：纯增量删除，不动其他逻辑
- [x] **不需要现场修复**：uninstall.sh 每次从源仓库调用

---

## 元发现 / Backlog 补充

### M1 · install ↔ uninstall 不对称

→ 已加入 `backlog/proposals.md`：v1.0.9+ 考虑 manifest.json 跟踪安装文件，让卸载从清单倒推（一次性根治"漏加"）。

### M2 · dogfooding 协议补充

→ 加新 skill / hook / agent 时，**同步更新 uninstall.sh + 跑一次 install→uninstall 循环测试**。下次提交前手测。

### M3 · validate.sh 已经覆盖了三类元数据，但没覆盖"卸载清洁度"

考虑 v1.0.9+ 在 validate.sh 加一个 `--cycle` 模式：在 /tmp 跑完整装-卸载循环，确认无残留。

---

## 门控

```
状态: 🟢 PASS（机器实测全绿，无需用户验证）
```

---

## 三连击 + 收尾全景

```
v1.0.5 修 settings 漂移（外部用户报错触发）
v1.0.6 加 settings 自检（局部预防）
v1.0.7 横向审计 → 修 skill/agent 漂移 + 自检扩展（全局预防）
v1.0.8 横向审计 → 修 uninstall 残留（脚本层完整性）
        ↑
        主动审计未回归脚本，发现自 v1.0.2 起的累积残留
```

至此：
- ✅ 三类元数据文件 schema 全对齐官方
- ✅ validate.sh 覆盖三类 schema 防回归
- ✅ install→uninstall 循环干净
- 🟡 stock_make_money 上的实战仍待用户验证

---

*Harness-Lite v1.0.8 · 路径C · 阶段4/4 · ✅ 完成*
