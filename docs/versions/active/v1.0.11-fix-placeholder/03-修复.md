# 03-修复.md · v1.0.11

> **路径C · 阶段3/4**

---

## 改动清单

| 文件 | 类型 | 改动 |
|-----|-----|-----|
| `scripts/init.sh` | 修改 | 渲染列表加 `{{HARNESS_LITE_DOCS_URL}}` 一行 |
| `scripts/upgrade.sh` | 修改 | 覆盖 rules 后立即渲染 principles/domain-rules 的版本占位符 |
| `scripts/validate.sh` | 修改 | 占位符扫描从单文件特例 → 全量扫 CLAUDE.md + rules/*.md，并显示哪个文件哪个占位符 |
| stock_make_money 现场 | 同步 | CLAUDE.md + domain-rules.md 渲染修复 |
| `VERSION` | 1.0.10 → 1.0.11 | bump |
| `CHANGELOG.md` | 追加 [1.0.11] | |

---

## 关键 diff · init.sh

```diff
+ local docs_url="https://github.com/zoe/harness-lite/blob/main/README.md"
  sed -i.bak \
      ... \
      -e "s#{{HARNESS_LITE_VERSION}}#$HARNESS_LITE_VERSION#g" \
+     -e "s#{{HARNESS_LITE_DOCS_URL}}#$docs_url#g" \
      -e "s#{{INSTALL_DATE}}#$install_date#g" \
      ...
```

## 关键 diff · upgrade.sh

```diff
  for rule_file in "$HARNESS_LITE_ROOT/templates/.claude/rules"/*.md; do
      ... 覆盖逻辑 ...
  done

+ # 覆盖完 rules 后，渲染新文件中的版本占位符
+ local upgrade_date=$(date +"%Y-%m-%d")
+ for rendered in ".claude/rules/principles.md" ".claude/rules/domain-rules.md"; do
+     if [ -f "$rendered" ]; then
+         sed -i.bak \
+             -e "s#{{HARNESS_LITE_VERSION}}#$NEW_VERSION#g" \
+             -e "s#{{INSTALL_DATE}}#$upgrade_date#g" \
+             "$rendered"
+         rm -f "$rendered.bak"
+     fi
+ done
```

## 关键 diff · validate.sh

```diff
- if [ -f ".claude/rules/tech-stack-rules.md" ]; then
-     if grep -q "{{" ".claude/rules/tech-stack-rules.md"; then
-         echo -e "${YELLOW}⚠${NC}  tech-stack-rules.md 中仍有未渲染的变量 {{...}}"
-     ...
-     fi
- fi

+ # 全量扫 CLAUDE.md / rules 中残留的占位符（防 v1.0.11 类回归）
+ set +e
+ unrendered=$(grep -lE "\{\{[A-Z_]+\}\}" CLAUDE.md .claude/rules/*.md 2>/dev/null)
+ set -e
+ if [ -n "$unrendered" ]; then
+     echo "✗ 以下文件含未渲染的 {{...}} 占位符："
+     echo "$unrendered" | while read f; do
+         ph=$(grep -oE "\{\{[A-Z_]+\}\}" "$f" 2>/dev/null | sort -u | tr '\n' ' ')
+         echo "      $f → $ph"
+     done
+ ...
```

精度从"单文件特例 + 笼统 warn"提升为"全量扫描 + 精确报告每个文件每个占位符"。

---

## 自检（机器实测）

### 测试 1 · 装完干净
```
$ cd /tmp/test && init.sh
$ validate.sh
✅ 所有检查通过！
```

### 测试 2 · 反向（注入坏数据）
```
$ sed -i 's#https://.../README.md#{{HARNESS_LITE_DOCS_URL}}#' CLAUDE.md
$ validate.sh
✗ 以下文件含未渲染的 {{...}} 占位符：
      CLAUDE.md → {{HARNESS_LITE_DOCS_URL}}
✗ 发现 1 个问题
```

### 测试 3 · stock_make_money 现场
```
修前：CLAUDE.md 残留 1 处 + domain-rules.md 残留 2 处
现场 sed 修复 → validate.sh 全绿 ✅
```

---

*Harness-Lite v1.0.11 · 路径C · 阶段3/4*
