# 03-修复.md · v1.0.24

> **路径C · 阶段3/4**

---

## 改动清单（7 文件 +180 行）

| 文件 | 改动 | 行数 |
|-----|-----|------|
| `templates/.claude/rules/principles.md` | +§13.1.1 决策点识别示例（8 场景）+ §13.1.2 自检清单 | +110 |
| `templates/.claude/skills/harness-req/SKILL.md` | +v1.0.24 实施层强约束段（详细）| +12 |
| `templates/.claude/skills/harness-design/SKILL.md` | +v1.0.24 强约束段（精简） | +9 |
| `templates/.claude/skills/harness-impact/SKILL.md` | +v1.0.24 强约束段（精简） | +5 |
| `templates/.claude/skills/harness-review/SKILL.md` | +v1.0.24 强约束段（精简） | +5 |
| `templates/.claude/skills/harness-test-ci/SKILL.md` | +v1.0.24 强约束段（精简） | +5 |
| `.gitignore` | + 处理符号链接边界（templates/.claude/...） | +3 |

---

## 关键代码

### principles.md §13.1.1（8 个识别示例）

```markdown
### 13.1.1 决策点识别示例（v1.0.24+ 重要 · 防 M1 实施 gap）

#### 场景 1 · AskUserQuestion 工具调用即决策点
AI 调用 AskUserQuestion(...)
→ ✅ 这就是决策点
→ AI 必须 append decision-log

#### 场景 2 · 在产物中写"X 还是 Y"对比表
AI 写 D1 表格 | 选项 A / 选项 B | 选 A | 理由
→ ✅ 这就是决策点（且 AI 自决了）
→ AI 应在产物末尾标注 + append log

... (场景 3-8: 来回讨论 / N 选 1 / 阈值 / 命名 / yes-no / 隐式决策)
```

### §13.1.2 自检清单

```markdown
AI 在每次产物输出前，对照自检：

[ ] 我是否调过 AskUserQuestion？           → 是 ✅ 写 log
[ ] 产物里是否含"X 还是 Y"对比表？           → 是 ✅ 写 log
[ ] 对话历史是否含 ≥2 轮"方向讨论"？        → 是 ✅ 写 log
[ ] 我是否在 N 个候选中拍板了一个？           → 是 ✅ 写 log
[ ] 有没有数字 / 命名 / 路径选择？           → 是 ✅ 写 log
[ ] 有没有 yes/no 决策？                    → 是 ✅ 写 log

任一为是 → decision-log 必须写
```

### 5 skill body 加强约束段

详细版（harness-req）+ 精简版（其他 4 个，引用 §13.1.1）。

### .gitignore 符号链接 bug 修复

```diff
.claude/decision-log.md
.claude/auto-decide.md
+# v1.0.24 · 处理符号链接边界（H-L 仓库 dogfooding 时 .claude → templates/.claude）
+templates/.claude/decision-log.md
+templates/.claude/auto-decide.md
```

---

## 实测验证：协议**首次真触发**

修复后立即在 H-L 仓库自身**写第一条 decision-log**：

```bash
$ cat .claude/decision-log.md
## 2026-06-18 11:45 · v1.0.24-protocol-implementation-gap · path-c · 修复强度
- 问题：v1.0.24 修复强度选哪个？
- 选项：[A 软 / B 中 / C 强 / D 推荐]
- 用户答：A 补示例纲领（软）
- 关键词：v1.0.24, M1 修复强度, 协议落地
- 状态：⏳ 待复盘

## 2026-06-18 11:30 · v1.0.24 · 验证场景 · multi-team-demo 设计
- AI 自决：B（审视过去版本）+ A（造场景但承认无法公正自测）
- ...

## 2026-06-18 11:35 · v1.0.24 · 修复方案
- 用户答：A
- ...
```

**3 条记录**——这是 H-L 仓库**首次真触发**协议自动写入。

---

## 自检（基于 principles §13.1.2）

- [x] 我调过 AskUserQuestion（v1.0.24 修复强度）→ ✅ 写了 log
- [x] 产物含"X 还是 Y"对比（修复强度 A/B/C/D）→ ✅ 写了 log
- [x] AI 自决了 N 选 1（验证方法）→ ✅ 写了 log

**任一为是都已记录**——v1.0.24 自身严格遵循 §13.1.2 自检。

---

## 现场修复

### multi-team-demo（验证场景）

`examples/multi-team-demo/` 完整保留作为后续 dogfooding 测试基底。

### stock 现场同步

需要将以下文件同步到 stock：
- `principles.md`（含 §13.1.1 / §13.1.2）
- 5 个 skill SKILL.md（含 v1.0.24 强约束段）

**注**：stock 不需要 .gitignore（它没有符号链接问题）。

---

## 不做（避免过度治理）

- ❌ 不加 SubagentStop hook（C 选项 · 用户拍板软约束）
- ❌ 不引入工具层绑定
- ❌ 不修改 v1.0.21 / v1.0.23 协议本身（只补示例 / 自检）

---

*Harness-Lite v1.0.24 · 路径C · 阶段3/4*
