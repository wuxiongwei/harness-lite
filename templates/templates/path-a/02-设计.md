# 02-设计.md

> **路径A · 完整需求 · 阶段2/5**
> 受众：团队（集体评审）+ 执行者本人（自审细节）
> 目的：把需求翻译为可执行的技术方案

---

## 元信息

- **版本**：v{{VERSION}}-{{SLUG}}
- **创建日期**：{{DATE}}
- **创建人**：{{AUTHOR}}
- **追溯**：[01-需求.md](01-需求.md)
- **状态**：⬜ 草稿 / 🟡 待评审 / 🟢 已确认 / 🔴 已阻塞

---

## 1. 方案概述（3句话以内）

```
（用一段话讲清楚怎么做）
```

---

## 2. 关键决策点（含为什么）

> 每个决策点：选项 + 优劣 + 选择 + 原因
> 这是未来"代码考古"的最重要参考

### 决策1：{标题}

| 选项 | 优势 | 劣势 |
|-----|-----|-----|
| 方案A | ... | ... |
| 方案B | ... | ... |

**选择**：方案A
**原因**：（为什么选A不选B）

### 决策2：{标题}

（同上格式）

### 决策3：{标题}

（同上格式）

---

## 3. 架构设计

### 3.1 模块结构

```
（用ASCII图或描述展示模块关系）

模块A
  └── 子模块A1
       └── 类/函数
```

### 3.2 数据流

```
用户请求
  ↓
入口层
  ↓
业务层
  ↓
数据层
  ↓
返回
```

### 3.3 核心类/函数

| 类/函数 | 路径 | 职责 |
|-------|-----|-----|
| ClassA | path/to/A.java | ... |
| funcB | path/to/B.ts | ... |

---

## 4. 接口契约

### 4.1 新增接口

```yaml
- method: POST
  path: /api/users
  description: 创建用户
  request:
    body:
      name: string (required)
      age: integer (optional, default 0)
  response:
    success:
      code: 0
      data:
        id: string
        name: string
    error:
      code: 400 | 401 | 500
```

### 4.2 修改接口

```yaml
- method: GET
  path: /api/users/{id}
  changes:
    - 新增字段：avatar (string, optional)
  backward_compatible: true / false
  migration: （如何迁移老调用方）
```

### 4.3 内部依赖

- 调用了哪些内部服务？
- 调用了哪些第三方接口？

---

## 5. 数据设计

### 5.1 数据模型

```sql
-- 新增表
CREATE TABLE users (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100),
    ...
);

-- 修改表
ALTER TABLE users ADD COLUMN avatar VARCHAR(255);
```

### 5.2 数据迁移

- [ ] 是否需要数据迁移？
- 迁移方案：
- 迁移脚本：
- 回滚方案：

### 5.3 兼容性

- 老数据如何兼容？
- 老接口如何兼容？

---

## 6. 边界场景设计

> 对应 01-需求.md 的边界场景，给出具体处理方案

### 边界1：异常输入

- 触发条件：
- 处理方式：
- 错误码：

### 边界2：并发情况

- 触发条件：
- 处理方式：（乐观锁/悲观锁/分布式锁）
- 影响范围：

### 边界3：权限不足

- 触发条件：
- 处理方式：
- 错误返回：

### 边界4：数据为空

- 触发条件：
- 处理方式：
- 默认值：

---

## 7. 不做的范围（继承自需求）

承接 01-需求.md 的"不做的范围"，**在设计层面再次明确**：

- ❌ 不引入：（不引入的依赖）
- ❌ 不改造：（不改造的老模块）
- ❌ 不优化：（不顺手优化的部分）

---

## 8. 影响面分析

### 8.1 文件清单

```
预计改动文件：
- 新增：
  - path/to/new1.java
  - path/to/new2.java
- 修改：
  - path/to/existing1.java
  - path/to/existing2.java
- 删除：
  - path/to/obsolete.java
```

### 8.2 老代码影响

- 哪些既有代码会被牵连？
- 是否有破坏性变更？
- 测试覆盖是否够？

### 8.3 跨模块影响

- 影响哪些上游模块？
- 影响哪些下游模块？
- 影响哪些跨入口（HTTP/MQ/定时任务/飞书事件）？

---

## 9. 风险与缓解

| 风险 | 概率 | 影响 | 缓解方案 |
|-----|-----|-----|---------|
| 风险1 | 高/中/低 | 高/中/低 | ... |
| 风险2 | ... | ... | ... |

---

## 10. 反向校验（提交前必填）

执行者本人 review 时回答：

- [ ] **追溯校验**：每个设计决策能追溯到 01-需求 的某条吗？
  > 决策1 ← 需求 §X.Y
  > 决策2 ← 需求 §X.Y

- [ ] **覆盖校验**：01-需求 的所有验收标准在设计中是否都有对应实现？
  > AC-1 → 由 ClassA 实现
  > AC-2 → 由 funcB 实现
  > ...

- [ ] **越界校验**：设计中是否有 01-需求 没要求的功能？
  > 检查结果：（无 / 有：列出来）

- [ ] **边界校验**：所有边界场景都有处理方案吗？
  > 边界1：✅ / 边界2：✅ / ...

---

## 11. 关联文档

- 上游：[01-需求.md](01-需求.md)
- 下游：[03-代码索引.md](03-代码索引.md) （写完代码后维护）
- 关联wiki：[[相关架构决策]] [[相关接口规范]]

---

## 门控信号

```
状态: 🟢 PASS / 🟡 REVISION / 🔴 BLOCKED
检查时间: {{TIMESTAMP}}
检查人: {{REVIEWER}}
```

### 通过条件（人介入1：方案确认）
- [ ] 团队评审通过（技术路线/边界/迭代拆分确认）
- [ ] 反向校验四项全部Yes
- [ ] 接口契约明确
- [ ] 风险已识别且有缓解方案

### 阻塞原因（如有）
（如状态为 BLOCKED）

---

## 下一步

- ✅ 状态 = 🟢 → 进入阶段3：[03-代码索引.md](03-代码索引.md)
- 🟡 状态 = 🟡 → 修订后重新评审
- 🔴 状态 = 🔴 → 等待阻塞解除

---

*Harness-Lite v{{HARNESS_LITE_VERSION}} · 路径A · 阶段2/5*
